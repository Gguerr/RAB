<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "RAB - Panel Administrativo" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 font-sans antialiased">
    <div class="flex min-h-screen">
      <!-- Sidebar -->
      <aside class="w-64 bg-white shadow-lg border-r border-gray-200 flex flex-col">
        <!-- Sidebar Header -->
        <div class="p-6 border-b border-gray-200 text-center">
          <%= image_tag "logo_Rab.png", alt: "RAB Logo", class: "w-12 h-auto rounded-lg mx-auto mb-2" %>
          <h2 class="text-wine-primary text-xl font-bold">RAB Admin</h2>
        </div>

        <!-- Navigation -->
        <nav class="flex-1 py-4 overflow-y-auto">
          <%= link_to admin_root_path, class: "flex items-center px-6 py-3 text-gray-700 hover:bg-gray-50 hover:text-wine-primary transition-colors duration-200 #{'bg-gray-50 text-wine-primary border-r-4 border-wine-primary font-semibold' if current_page?(admin_root_path)}" do %>
            <span class="text-xl mr-3">📊</span>
            <span>Dashboard</span>
          <% end %>

          <!-- Management Section -->
          <div class="mt-6">
            <h3 class="px-6 py-2 text-xs font-semibold text-gray-500 uppercase tracking-wider">Gestión</h3>
            
            <%= link_to admin_employees_path, class: "flex items-center px-6 py-3 text-gray-700 hover:bg-gray-50 hover:text-wine-primary transition-colors duration-200 #{'bg-gray-50 text-wine-primary border-r-4 border-wine-primary font-semibold' if controller_name == 'employees'}" do %>
              <span class="text-xl mr-3">👥</span>
              <span>Empleados</span>
            <% end %>
          </div>
        </nav>

        <!-- Sidebar Footer -->
        <div class="p-4 border-t border-gray-200">
          <div class="text-center">
            <p class="text-sm text-gray-600 mb-2"><%= current_admin.email %></p>
            <%= link_to "Cerrar Sesión", destroy_admin_session_path, 
                data: { "turbo-method": :delete }, 
                class: "text-wine-primary text-sm font-semibold hover:underline" %>
          </div>
        </div>
      </aside>

      <!-- Main Content -->
      <main class="flex-1 flex flex-col">
        <!-- Header -->
        <header class="bg-white shadow-sm border-b border-gray-200 px-6 py-4 flex justify-between items-center">
          <h1 class="text-2xl font-bold text-gray-800"><%= content_for(:page_title) || "Dashboard" %></h1>
          <div class="flex items-center space-x-3">
            <%= yield :header_actions %>
          </div>
        </header>

        <!-- Content Area -->
        <div class="flex-1 p-6">
          <!-- Flash Messages -->
          <% flash.each do |type, message| %>
            <div class="mb-6 p-4 rounded-lg <%= type == 'notice' ? 'bg-green-50 border border-green-200 text-green-800' : 'bg-red-50 border border-red-200 text-red-800' %>">
              <div class="flex items-center">
                <span class="mr-2 text-lg">
                  <%= type == 'notice' ? '✅' : '❌' %>
                </span>
                <%= message %>
              </div>
            </div>
          <% end %>

          <!-- Page Content -->
          <%= yield %>
        </div>
      </main>
    </div>

    <!-- Mobile Sidebar Overlay (for future mobile support) -->
    <div class="fixed inset-0 bg-black bg-opacity-50 z-40 lg:hidden hidden" id="sidebar-overlay"></div>

  </body>
</html>